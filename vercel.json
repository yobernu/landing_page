{
  "version": 2,
  "builds": [
    {
      "src": "vercel-build.sh",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".vercel/output/static"
      }
    }
  ],
  "routes": [
    {
      "src": "^/(.*)$",
      "dest": "/index.html"
    }
  ]
}
